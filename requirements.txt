# SunKeyword 智能词库回复插件依赖

# 核心依赖 - 无外部依赖
# sqlite3 是 Python 标准库的一部分
# shutil 是 Python 标准库的一部分  
# typing 是 Python 标准库的一部分

# 说明: 此插件专为简洁性设计，仅使用 Python 标准库
# 无需安装额外的第三方包

# 如果需要扩展功能，可考虑以下可选依赖：
# sqlite-utils>=3.30.0  # SQLite 工具库（可选）
# aiofiles>=0.8.0       # 异步文件操作（可选）