# SunOS 插件依赖

# 数据库相关
# sqlite3 是 Python 标准库的一部分，但为了明确说明数据库依赖，这里列出
# sqlite3  # Python 内置模块

# 如果需要更高级的 SQLite 功能，可以使用以下库：
# sqlite-utils>=3.30.0  # SQLite 工具库（可选）
# sqlalchemy>=1.4.0     # ORM 框架（可选）

# 其他依赖
# typing_extensions>=4.0.0  # 类型注解扩展（Python 3.8+ 内置）
